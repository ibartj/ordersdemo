<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/MyActionBar</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MyActionBar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorIcons</item>
        <item name="android:textColorPrimaryInverse">@color/colorIcons</item>
        <item name="android:textColor">@color/colorIcons</item>
        <item name="android:actionMenuTextColor">@color/colorIcons</item>
        <item name="titleTextColor">@color/colorIcons</item>
        <item name="android:colorControlNormal">@color/colorIcons</item>
    </style>

</resources>
